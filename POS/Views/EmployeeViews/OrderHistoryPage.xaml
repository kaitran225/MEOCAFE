<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="POS.Views.EmployeeViews.OrderHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POS.Views.EmployeeViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:POS.ViewModels"
    xmlns:model="using:POS.Models"
    xmlns:converters="using:POS.Helpers.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:PriceConverter x:Key="PriceConverter" />
    </Page.Resources>

    <Grid>
        <SplitView x:Name="OrderSplitView"
                   IsPaneOpen="False"
                   PanePlacement="Right"
                   PaneBackground="Transparent"
                   DisplayMode="Inline">
            <SplitView.Content>
                <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical">

                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button
                                x:Name="DeleteOrderBtn"
                                Margin="0,0,5,0"
                                Background="Red"
                                Click="DeleteOrderBtn_Click"
                                Content="Xóa"
                                Foreground="Black" />
                        </StackPanel>

                        <AutoSuggestBox
                            x:Name="SearchOrderBar"
                            Grid.Column="1"
                            Width="300"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            PlaceholderText="Type an Order ID"
                            QueryIcon="Find"/>
                    </Grid>

                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,10,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <CalendarDatePicker
                            x:Name="StartDatePicker"
                            Grid.Column="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            PlaceholderText="Start Date"
                            DateChanged="DatePicker_DateChanged" />

                        <CalendarDatePicker
                            x:Name="EndDatePicker"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            PlaceholderText="End Date"
                            DateChanged="DatePicker_DateChanged" />

                        <Button
                            x:Name="FilterButton"
                            Grid.Column="2"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            Content="Filter"
                            Click="FilterButton_Click" />
                    </Grid>

                    <Border
                        Margin="0,10,0,10"
                        BorderBrush="Gray"
                        BorderThickness="0,0,0,1" />
                    <!--  Divider  -->

                    <Grid x:Name="OrderGrid" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ScrollViewer
                            x:Name="ScrollViewListOrder"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"
                            HorizontalScrollBarVisibility="Auto"
                            HorizontalScrollMode="Enabled"
                            IsTabStop="True"
                            IsVerticalScrollChainingEnabled="True"
                            VerticalScrollBarVisibility="Auto"
                            VerticalScrollMode="Enabled"
                            ZoomMode="Enabled">

                            <ListView
                                x:Name="ListOrder"
                                Height="500"
                                ItemsSource="{x:Bind ViewModel.FilteredOrders, Mode=OneWay}">
                                <!--  *******************HEADER***************  -->
                                <ListView.Header>
                                    <Grid Padding="16,6" ColumnSpacing="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="50" />
                                            <ColumnDefinition Width="0.4*" />
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="1.5*" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox
                                            x:Name="OrderHeaderCheckBox"
                                            Grid.Column="0"
                                            Width="10"
                                            Padding="0,0,1,0"
                                            HorizontalAlignment="Stretch"
                                            Checked="OrderHeaderCheckBox_Checked"
                                            FontSize="10"
                                            Unchecked="OrderHeaderCheckBox_Unchecked" />

                                        <TextBlock
                                            Grid.Column="1"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="ID" />
                                        <TextBlock
                                            Grid.Column="2"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Tổng thanh toán" />
                                        <TextBlock
                                            Grid.Column="3"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Ghi chú" />
                                        <TextBlock
                                            Grid.Column="4"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Số điện thoại" />
                                        <TextBlock
                                            Grid.Column="5"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Ngày đặt" />
                                        <TextBlock
                                            Grid.Column="6"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Nhân viên" />
                                        <TextBlock
                                            Grid.Column="7"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            Text="Action" />
                                    </Grid>
                                </ListView.Header>

                                <ListView.ItemTemplate>
                                    <!--  *******************DATA***************  -->

                                    <DataTemplate x:DataType="model:Order">
                                        <Grid ColumnSpacing="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="50" />
                                                <ColumnDefinition Width="0.4*" />
                                                <ColumnDefinition Width="1.5*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="1.5*" />
                                                <ColumnDefinition Width="1.5*" />
                                                <ColumnDefinition Width="1.5*" />
                                                <ColumnDefinition Width="1.5*" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" Checked="OrderListItemCheckBox_Checked" Unchecked="OrderListItemCheckBox_Unchecked"  />
                                            <TextBlock Grid.Column="1" Text="{x:Bind Id}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{x:Bind GrandTotal, Converter={StaticResource PriceConverter}}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{x:Bind Note}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{x:Bind PhoneNumber}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="5" Text="{x:Bind CreatedAt}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="6" Text="{x:Bind Employee.Fullname}" VerticalAlignment="Center" />
                                            <Button Grid.Column="7" Content="View" Click="ViewOrderDetails_Click" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>

                                <ListView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </ScrollViewer>
                    </Grid>

                    <StackPanel
                        Margin="0,10,0,10"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            Click="PrevOrderPage_Click"
                            Content="Trước"
                            IsEnabled="{x:Bind HasPreviousPage, Mode=OneWay}" />
                        <TextBlock
                            Margin="10,0,10,0"
                            VerticalAlignment="Center"
                            Text="{x:Bind PageInfo, Mode=OneWay}" />
                        <Button
                            Click="NextOrderPage_Click"
                            Content="Sau"
                            IsEnabled="{x:Bind HasNextPage, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>
            </SplitView.Content>

            <SplitView.Pane>
                <Grid Margin="20">
                    <StackPanel x:Name="OrderDetailsPanel" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="Chi tiết đơn hàng" FontSize="24" FontWeight="Bold" />
                            <Button Margin="30,3,0,0" BorderThickness="0" Click="CloseOrderDetailsPanelButton_Click">
                                <StackPanel>
                                    <SymbolIcon Symbol="Cancel" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <!-- Add your order details controls here -->
                        <TextBlock x:Name="OrderDetailsTextBlock" Text="Order details will be shown here." />
                    </StackPanel>
                </Grid>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>
